# API 서버 base URL
# Vite는 이 값을 빌드 시점에 번들에 박아 넣는다 - 반드시 `npm run build` 실행 전에 설정할 것.
# 로컬 개발: http://localhost:4000 (미설정 시 기본값이라 로컬에선 안 넣어도 됨)
# 배포: https://api.dongbanhagae.com
VITE_API_BASE_URL=https://api.dongbanhagae.com
